apiVersion: v1
kind: Service
metadata:
  name: ai-autopilot
  labels:
    app: ai-autopilot
spec:
  type: ClusterIP
  ports:
    - name: health
      port: 5080
      targetPort: 5080
      protocol: TCP
  selector:
    app: ai-autopilot
