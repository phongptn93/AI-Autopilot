apiVersion: v1
kind: Service
metadata:
  name: ado-autopilot
  labels:
    app: ado-autopilot
spec:
  type: ClusterIP
  ports:
    - name: health
      port: 5080
      targetPort: 5080
      protocol: TCP
  selector:
    app: ado-autopilot
